{
  "name": "pipecat-railway-template",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild client.js --bundle --minify --format=esm --outfile=static/app.js",
    "check": "node --check client.js",
    "test": "node --test tests/test_client.mjs"
  },
  "dependencies": {
    "@daily-co/daily-js": "0.92.2"
  },
  "devDependencies": {
    "esbuild": "0.28.2",
    "railway": "3.11.0"
  }
}
